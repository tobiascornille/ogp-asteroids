- Is it okay to iterate over the whole .values() set to remove an entity?
- Is it okay to update the hashmap each time the evolve method is called?
- Is it okay to use a deque for bullets
- Is it okay to set the world etc in facade?
- Is it okay to do nothing when a ship in an unbounded space fires a bullet?
- How do we implement the 99-101 rule in getTimeToCollision?
- Can 2 collisions occur at exactly the same time?
- Can we still use move?
- Why don't the sounds work?
